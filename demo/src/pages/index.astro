---
import { Layout, Header, Map, Tags, Footer } from 'astro-travelens'
import { SiteMetadata } from 'astro-travelens/config'
---

<Layout title={SiteMetadata.title}>
	<Header />
	<Tags tags={["a", "b", "c"]} />
	<Map text="Devil's Corner" lat={-41.97224} lng={148.15147} zoom={17} />
	<Footer />
</Layout>
